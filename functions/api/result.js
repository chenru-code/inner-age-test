import{cookies,json,sha256,verifySession}from"../lib/security.js";
const dimensionOrder=["curiosity","responsibility","stability","social","acceptance","adaptability","responsibility","curiosity","stability","social","acceptance","adaptability","curiosity","responsibility","stability","social","adaptability","acceptance","responsibility","acceptance"],names={curiosity:"好奇心",stability:"情绪稳定",social:"社交开放",responsibility:"责任担当",adaptability:"适应力",acceptance:"自我接纳"};
function ageFromBirth(birth){const b=new Date(`${birth}T00:00:00`),n=new Date();if(Number.isNaN(b.getTime()))return-1;let age=n.getFullYear()-b.getFullYear();if(n.getMonth()<b.getMonth()||n.getMonth()===b.getMonth()&&n.getDate()<b.getDate())age--;return age}
export async function onRequestPost({request,env}){const c=cookies(request),session=await verifySession(c.ia_session,env.SESSION_SECRET||"");if(!session||session.scope!=="test"||!c.ia_device||await sha256(c.ia_device)!==session.deviceHash)return json({message:"测试凭证已过期，请返回首页重新验证邀请码"},401);const{birth,answers}=await request.json(),actual=ageFromBirth(String(birth||""));if(actual<10||actual>100||!Array.isArray(answers)||answers.length!==20||answers.some(v=>!Number.isInteger(v)||v<0||v>3))return json({message:"答题数据不完整，请重新测试"},400);const sums={},counts={};Object.keys(names).forEach(k=>{sums[k]=0;counts[k]=0});dimensionOrder.forEach((d,i)=>{sums[d]+=answers[i]+1;counts[d]++});const dims={};Object.keys(names).forEach(d=>dims[d]=Math.round(35+(sums[d]/counts[d]-1)*20));const avg=Object.values(dims).reduce((a,b)=>a+b,0)/6,mental=Math.max(16,Math.min(72,Math.round(actual+(avg-65)*.24+(dims.responsibility-dims.curiosity)*.07))),score=Math.round(Object.values(dims).reduce((a,b)=>a+b,0)*160/600);let title="从容成长的探索者",identity="清醒而温暖的同行者";if(dims.responsibility>78&&dims.stability>70){title="稳健进取的实干家";identity="有担当的长期主义者"}else if(dims.curiosity>78&&dims.adaptability>72){title="永远年轻的探索家";identity="带着热望的生活实验者"}else if(dims.social>78&&dims.acceptance>72){title="温暖通透的连接者";identity="细腻而坚定的共情者"}else if(avg<58){title="正在长出力量的青年";identity="敏感而真诚的成长者"}const sorted=Object.keys(dims).sort((a,b)=>dims[b]-dims[a]);return json({actual,mental,score,dims,title,identity,keywords:sorted.slice(0,4).map(d=>`#${names[d]}`)})}
